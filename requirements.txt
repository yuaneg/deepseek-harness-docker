# 数值计算底层
numpy==1.26.4
# 数据分析核心
pandas==2.2.2

# Excel读写套件
openpyxl==3.1.5
xlsxwriter==3.2.0
xlrd==2.0.1

# 日期拓展处理
python-dateutil==2.9.0

# 可视化绘图
matplotlib==3.9.2
seaborn==0.13.2

# HTTP请求（适配你CRM导出脚本）
requests==2.32.3

# 中文文本分词（表格文本分析）
jieba==0.42.1

# 高性能大数据Excel（百万行可选，不用可删掉）
polars==0.20.31

# ── 常用扩展 ──

# 科学计算 / 统计 / 优化
scipy==1.13.1

# YAML 解析（配置文件）
pyyaml==6.0.2

# HTML / XML 解析（爬虫）
beautifulsoup4==4.12.3
lxml==5.3.0

# 现代 HTTP 客户端（支持异步）
httpx==0.27.2